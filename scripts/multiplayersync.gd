extends FlowContainer
# This script literally only used for variables that might need to be passed on.
@export var cards: Array = []
@export var first_card = true
@export var wild_current_color = "red"
@export var turn = 1 # 1 = server, other = client
